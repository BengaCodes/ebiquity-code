export const aggregateData = arr => arr.reduce((acc, value) => acc + value, 0)

export const dataColors = ['#FF0000', '#EAC100', '#2940D3', '#54E346']

export const defaultLabelStyle = {
  fontSize: '2px',
  fontFamily: 'sans-serif',
}